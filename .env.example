# Local Compose demo. Copy this file to .env.
SITE_ADDRESS=http://localhost
BIND_ADDRESS=127.0.0.1
HTTP_PORT=8088
HTTPS_PORT=8443
# On your VM: SITE_ADDRESS=relay.your-domain.com, BIND_ADDRESS=0.0.0.0,
# HTTP_PORT=80, HTTPS_PORT=443. DNS must point to the VM before enabling HTTPS.
